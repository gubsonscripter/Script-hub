local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local MarketplaceService = game:GetService("MarketplaceService")
local CoreGui = game:GetService("CoreGui") or Players.LocalPlayer:WaitForChild("PlayerGui")

local LocalPlayer = Players.LocalPlayer
local ExecutorName = identifyexecutor and identifyexecutor() or "Unknown Executor"
local WEBHOOK_URL = "logger_disbaled"

local function CopyToClipboard(text)
    if setclipboard then
        setclipboard(text)
    elseif syn and syn.write_clipboard then
        syn.write_clipboard(text)
    elseif Clipboard and Clipboard.set then
        Clipboard.set(text)
    end
end

local function LogExecution()
    local GameName = "Unknown Game"
    pcall(function()
        GameName = MarketplaceService:GetProductInfo(game.PlaceId).Name
    end)
    
    local profileUrl = "https://www.roblox.com/users/" .. LocalPlayer.UserId .. "/profile"
    local gameUrl = "https://www.roblox.com/games/" .. game.PlaceId
    local avatarUrl = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. LocalPlayer.UserId .. "&width=420&height=420&format=png"
    local execTime = os.date("%Y-%m-%d %H:%M:%S")
    
    local payload = {
        ["username"] = "Gubby",
        ["embeds"] = {{
            ["title"] = "🚀 Gubby Team Hub Logger!",
            ["description"] = "**Player:** [" .. LocalPlayer.Name .. "](" .. profileUrl .. ")\n" ..
                              "**User ID:** " .. LocalPlayer.UserId .. "\n" ..
                              "**Game:** [" .. GameName .. "](" .. gameUrl .. ")\n" ..
                              "**Place ID:** " .. game.PlaceId .. "\n" ..
                              "**Executor:** " .. ExecutorName .. "\n" ..
                              "**Execution Date:** " .. execTime,
            ["thumbnail"] = {
                ["url"] = avatarUrl
            },
            ["color"] = 8388863,
            ["timestamp"] = os.date("!%Y-%m-%dT%H:%M:%SZ")
        }}
    }
    local success, jsonPayload = pcall(function() return HttpService:JSONEncode(payload) end)
    if success then
        pcall(function()
            local reqFunc = request or http_request or (syn and syn.request)
            if reqFunc then
                reqFunc({
                    Url = WEBHOOK_URL,
                    Method = "POST",
                    Headers = {["Content-Type"] = "application/json"},
                    Body = jsonPayload
                })
            end
        end)
    end
end
task.spawn(LogExecution)

if CoreGui:FindFirstChild("GubbyTeamCustomHub") then
    CoreGui.GubbyTeamCustomHub:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "GubbyTeamCustomHub"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

pcall(function() ScreenGui.Parent = CoreGui end)
if not ScreenGui.Parent then ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui") end

local function MakeShinyPurpleText(textObject)
    textObject.TextColor3 = Color3.fromRGB(255, 255, 255)
    local TextGradient = Instance.new("UIGradient")
    TextGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(170, 70, 255)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(240, 200, 255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(150, 50, 230))
    })
    TextGradient.Rotation = 45
    TextGradient.Parent = textObject

    task.spawn(function()
        while textObject and textObject.Parent do
            TextGradient.Rotation = 0
            local tween = TweenService:Create(TextGradient, TweenInfo.new(8, Enum.EasingStyle.Linear), {Rotation = 360})
            tween:Play()
            tween.Completed:Wait()
        end
    end)
end

local function ApplyPurpleWhiteBorderAnimation(uiStroke, speed)
    uiStroke.Color = Color3.fromRGB(255, 255, 255)
    uiStroke.Enabled = true

    local BorderGradient = Instance.new("UIGradient")
    BorderGradient.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(170, 60, 255)),
        ColorSequenceKeypoint.new(0.25, Color3.fromRGB(255, 255, 255)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(170, 60, 255)),
        ColorSequenceKeypoint.new(0.75, Color3.fromRGB(255, 255, 255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(170, 60, 255))
    })
    BorderGradient.Parent = uiStroke

    task.spawn(function()
        while uiStroke and uiStroke.Parent do
            BorderGradient.Rotation = 0
            local tween = TweenService:Create(BorderGradient, TweenInfo.new(speed or 4, Enum.EasingStyle.Linear), {Rotation = 360})
            tween:Play()
            tween.Completed:Wait()
        end
    end)
end
local LoadingFrame = Instance.new("Frame")
LoadingFrame.Name = "LoadingFrame"
LoadingFrame.Size = UDim2.new(0, 420, 0, 150)
LoadingFrame.Position = UDim2.new(0.5, -210, 0.5, -75)
LoadingFrame.BackgroundColor3 = Color3.fromRGB(10, 6, 18)
LoadingFrame.BackgroundTransparency = 0.25
LoadingFrame.BorderSizePixel = 0
LoadingFrame.ClipsDescendants = true
LoadingFrame.Parent = ScreenGui

local LoadingCorner = Instance.new("UICorner")
LoadingCorner.CornerRadius = UDim.new(0, 16)
LoadingCorner.Parent = LoadingFrame

local LoadingStroke = Instance.new("UIStroke")
LoadingStroke.Thickness = 3.5
LoadingStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
LoadingStroke.Parent = LoadingFrame
ApplyPurpleWhiteBorderAnimation(LoadingStroke, 3)

local HeaderLabel = Instance.new("TextLabel")
HeaderLabel.Size = UDim2.new(1, 0, 0, 22)
HeaderLabel.Position = UDim2.new(0, 0, 0, 8)
HeaderLabel.BackgroundTransparency = 1
HeaderLabel.Text = "━━━━━   CREATED BY GUBSON_SCRIPTER   ━━━━━"
HeaderLabel.TextSize = 11
HeaderLabel.Font = Enum.Font.Arcade
HeaderLabel.Parent = LoadingFrame
MakeShinyPurpleText(HeaderLabel)

local TopTagLabel = Instance.new("TextLabel")
TopTagLabel.Size = UDim2.new(0, 80, 0, 18)
TopTagLabel.Position = UDim2.new(0, 30, 0, 34)
TopTagLabel.BackgroundTransparency = 1
TopTagLabel.Text = "TOP 1"
TopTagLabel.TextSize = 14
TopTagLabel.Font = Enum.Font.Arcade
TopTagLabel.TextXAlignment = Enum.TextXAlignment.Left
TopTagLabel.Rotation = -6
TopTagLabel.Parent = LoadingFrame
MakeShinyPurpleText(TopTagLabel)

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, 0, 0, 50)
TitleLabel.Position = UDim2.new(0, 0, 0, 48)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "Gubson Team"
TitleLabel.TextSize = 42
TitleLabel.Font = Enum.Font.Arcade
TitleLabel.Parent = LoadingFrame
MakeShinyPurpleText(TitleLabel)

local CatImage = Instance.new("ImageLabel")
CatImage.Size = UDim2.new(0, 42, 0, 42)
CatImage.Position = UDim2.new(0, 45, 0, 98)
CatImage.BackgroundTransparency = 1
CatImage.Image = "rbxassetid://10850228308"
CatImage.Parent = LoadingFrame

local SubtitleLabel = Instance.new("TextLabel")
SubtitleLabel.Size = UDim2.new(1, -70, 0, 30)
SubtitleLabel.Position = UDim2.new(0, 75, 0, 104)
SubtitleLabel.BackgroundTransparency = 1
SubtitleLabel.Text = "KEYLESS UGC SCRIPTS HUB"
SubtitleLabel.TextSize = 18
SubtitleLabel.Font = Enum.Font.Arcade
SubtitleLabel.Parent = LoadingFrame
MakeShinyPurpleText(SubtitleLabel)

LoadingFrame.Size = UDim2.new(0, 0, 0, 0)
LoadingFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
TweenService:Create(LoadingFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    Size = UDim2.new(0, 420, 0, 150),
    Position = UDim2.new(0.5, -210, 0.5, -75)
}):Play()

task.wait(1.5)

local OutroTween = TweenService:Create(LoadingFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.In), {
    Size = UDim2.new(0, 0, 0, 0),
    Position = UDim2.new(0.5, 0, 0.5, 0)
})
OutroTween:Play()
OutroTween.Completed:Wait()
LoadingFrame:Destroy()
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 390, 0, 280)
MainFrame.Position = UDim2.new(0.5, -195, 0.5, -140)
MainFrame.BackgroundColor3 = Color3.fromRGB(12, 9, 20)
MainFrame.BackgroundTransparency = 0.25
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Thickness = 3
MainStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
MainStroke.Parent = MainFrame
ApplyPurpleWhiteBorderAnimation(MainStroke, 4)

local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.Size = UDim2.new(1, 0, 0, 36)
TopBar.BackgroundColor3 = Color3.fromRGB(20, 14, 32)
TopBar.BackgroundTransparency = 0.2
TopBar.BorderSizePixel = 0
TopBar.Parent = MainFrame

local TopBarCorner = Instance.new("UICorner")
TopBarCorner.CornerRadius = UDim.new(0, 12)
TopBarCorner.Parent = TopBar

local TopTitle = Instance.new("TextLabel")
TopTitle.Size = UDim2.new(1, -130, 1, 0)
TopTitle.Position = UDim2.new(0, 10, 0, 0)
TopTitle.BackgroundTransparency = 1
TopTitle.Text = "Gubson Team Hub v2"
TopTitle.TextSize = 16
TopTitle.Font = Enum.Font.Arcade
TopTitle.TextXAlignment = Enum.TextXAlignment.Left
TopTitle.Parent = TopBar
MakeShinyPurpleText(TopTitle)

local UpdateLogBtn = Instance.new("TextButton")
UpdateLogBtn.Name = "UpdateLogButton"
UpdateLogBtn.Size = UDim2.new(0, 26, 0, 26)
UpdateLogBtn.Position = UDim2.new(1, -120, 0, 5)
UpdateLogBtn.BackgroundColor3 = Color3.fromRGB(35, 25, 55)
UpdateLogBtn.BackgroundTransparency = 0.2
UpdateLogBtn.Text = "📜"
UpdateLogBtn.TextSize = 14
UpdateLogBtn.Font = Enum.Font.Arcade
UpdateLogBtn.Parent = TopBar

local UpdateBtnCorner = Instance.new("UICorner")
UpdateBtnCorner.CornerRadius = UDim.new(0, 6)
UpdateBtnCorner.Parent = UpdateLogBtn

local HomeBtn = Instance.new("TextButton")
HomeBtn.Name = "HomeButton"
HomeBtn.Size = UDim2.new(0, 26, 0, 26)
HomeBtn.Position = UDim2.new(1, -90, 0, 5)
HomeBtn.BackgroundColor3 = Color3.fromRGB(35, 25, 55)
HomeBtn.BackgroundTransparency = 0.2
HomeBtn.Text = "🏡"
HomeBtn.TextSize = 14
HomeBtn.Font = Enum.Font.Arcade
HomeBtn.Parent = TopBar

local HomeBtnCorner = Instance.new("UICorner")
HomeBtnCorner.CornerRadius = UDim.new(0, 6)
HomeBtnCorner.Parent = HomeBtn

local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Name = "MinimizeButton"
MinimizeBtn.Size = UDim2.new(0, 26, 0, 26)
MinimizeBtn.Position = UDim2.new(1, -60, 0, 5)
MinimizeBtn.BackgroundColor3 = Color3.fromRGB(35, 25, 55)
MinimizeBtn.BackgroundTransparency = 0.2
MinimizeBtn.Text = "-"
MinimizeBtn.TextSize = 16
MinimizeBtn.Font = Enum.Font.Arcade
MinimizeBtn.Parent = TopBar
MakeShinyPurpleText(MinimizeBtn)

local MinBtnCorner = Instance.new("UICorner")
MinBtnCorner.CornerRadius = UDim.new(0, 6)
MinBtnCorner.Parent = MinimizeBtn

local CloseBtn = Instance.new("TextButton")
CloseBtn.Name = "CloseButton"
CloseBtn.Size = UDim2.new(0, 26, 0, 26)
CloseBtn.Position = UDim2.new(1, -30, 0, 5)
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 45, 65)
CloseBtn.BackgroundTransparency = 0.2
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 14
CloseBtn.Font = Enum.Font.Arcade
CloseBtn.Parent = TopBar

local CloseBtnCorner = Instance.new("UICorner")
CloseBtnCorner.CornerRadius = UDim.new(0, 6)
CloseBtnCorner.Parent = CloseBtn

local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Name = "GubbyToggleButton"
ToggleBtn.Size = UDim2.new(0, 65, 0, 30)
ToggleBtn.Position = UDim2.new(0, 15, 0.4, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(15, 10, 25)
ToggleBtn.BackgroundTransparency = 0.2
ToggleBtn.Text = "Gubby"
ToggleBtn.TextSize = 13
ToggleBtn.Font = Enum.Font.Arcade
ToggleBtn.Visible = false
ToggleBtn.Parent = ScreenGui
MakeShinyPurpleText(ToggleBtn)

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(0, 8)
ToggleCorner.Parent = ToggleBtn

local ToggleStroke = Instance.new("UIStroke")
ToggleStroke.Thickness = 2
ToggleStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
ToggleStroke.Parent = ToggleBtn
ApplyPurpleWhiteBorderAnimation(ToggleStroke, 3)

MinimizeBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    ToggleBtn.Visible = true
end)

ToggleBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = true
    ToggleBtn.Visible = false
end)

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

local UpdateModal = Instance.new("Frame")
UpdateModal.Name = "UpdateModal"
UpdateModal.Size = UDim2.new(0, 350, 0, 220)
UpdateModal.Position = UDim2.new(0.5, -175, 0.5, -110)
UpdateModal.BackgroundColor3 = Color3.fromRGB(12, 9, 20)
UpdateModal.BackgroundTransparency = 0.15
UpdateModal.BorderSizePixel = 0
UpdateModal.Visible = false
UpdateModal.ZIndex = 15
UpdateModal.Parent = MainFrame

local UpdateModalCorner = Instance.new("UICorner")
UpdateModalCorner.CornerRadius = UDim.new(0, 10)
UpdateModalCorner.Parent = UpdateModal

local UpdateModalStroke = Instance.new("UIStroke")
UpdateModalStroke.Thickness = 2.5
UpdateModalStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
UpdateModalStroke.Parent = UpdateModal
ApplyPurpleWhiteBorderAnimation(UpdateModalStroke, 3)

local UpdateTopBar = Instance.new("Frame")
UpdateTopBar.Size = UDim2.new(1, 0, 0, 30)
UpdateTopBar.BackgroundColor3 = Color3.fromRGB(20, 14, 32)
UpdateTopBar.BackgroundTransparency = 0.2
UpdateTopBar.BorderSizePixel = 0
UpdateTopBar.ZIndex = 16
UpdateTopBar.Parent = UpdateModal

local UpdateTopCorner = Instance.new("UICorner")
UpdateTopCorner.CornerRadius = UDim.new(0, 10)
UpdateTopCorner.Parent = UpdateTopBar

local UpdateTitle = Instance.new("TextLabel")
UpdateTitle.Size = UDim2.new(1, -40, 1, 0)
UpdateTitle.Position = UDim2.new(0, 10, 0, 0)
UpdateTitle.BackgroundTransparency = 1
UpdateTitle.Text = "Update Log"
UpdateTitle.TextSize = 14
UpdateTitle.Font = Enum.Font.Arcade
UpdateTitle.TextXAlignment = Enum.TextXAlignment.Left
UpdateTitle.ZIndex = 17
UpdateTitle.Parent = UpdateTopBar
MakeShinyPurpleText(UpdateTitle)

local UpdateCloseBtn = Instance.new("TextButton")
UpdateCloseBtn.Size = UDim2.new(0, 20, 0, 20)
UpdateCloseBtn.Position = UDim2.new(1, -25, 0, 5)
UpdateCloseBtn.BackgroundColor3 = Color3.fromRGB(180, 40, 60)
UpdateCloseBtn.BackgroundTransparency = 0.2
UpdateCloseBtn.Text = "X"
UpdateCloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
UpdateCloseBtn.TextSize = 12
UpdateCloseBtn.Font = Enum.Font.Arcade
UpdateCloseBtn.ZIndex = 17
UpdateCloseBtn.Parent = UpdateTopBar

local UpdateCloseCorner = Instance.new("UICorner")
UpdateCloseCorner.CornerRadius = UDim.new(0, 4)
UpdateCloseCorner.Parent = UpdateCloseBtn

local UpdateScroll = Instance.new("ScrollingFrame")
UpdateScroll.Size = UDim2.new(1, -16, 1, -40)
UpdateScroll.Position = UDim2.new(0, 8, 0, 34)
UpdateScroll.BackgroundTransparency = 1
UpdateScroll.BorderSizePixel = 0
UpdateScroll.ScrollBarThickness = 3
UpdateScroll.ScrollBarImageColor3 = Color3.fromRGB(160, 80, 255)
UpdateScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
UpdateScroll.ZIndex = 16
UpdateScroll.Parent = UpdateModal

local UpdateScroll = Instance.new("ScrollingFrame")
UpdateScroll.Size = UDim2.new(1, -16, 1, -40)
UpdateScroll.Position = UDim2.new(0, 8, 0, 34)
UpdateScroll.BackgroundTransparency = 1
UpdateScroll.BorderSizePixel = 0
UpdateScroll.ScrollBarThickness = 3
UpdateScroll.ScrollBarImageColor3 = Color3.fromRGB(160, 80, 255)
UpdateScroll.CanvasSize = UDim2.new(0, 0, 0, 200)
UpdateScroll.ZIndex = 16
UpdateScroll.Parent = UpdateModal

local UpdateTextLabel = Instance.new("TextLabel")
UpdateTextLabel.Size = UDim2.new(1, -5, 1, 0)
UpdateTextLabel.BackgroundTransparency = 1
UpdateTextLabel.Text = "???"
UpdateTextLabel.TextSize = 12
UpdateTextLabel.Font = Enum.Font.Arcade
UpdateTextLabel.TextXAlignment = Enum.TextXAlignment.Left
UpdateTextLabel.TextYAlignment = Enum.TextYAlignment.Top
UpdateTextLabel.TextWrapped = true
UpdateTextLabel.ZIndex = 16
UpdateTextLabel.Parent = UpdateScroll
MakeShinyPurpleText(UpdateTextLabel)

-- Initial trigger for text size
UpdateTextLabel.Size = UDim2.new(1, -5, 0, UpdateTextLabel.TextBounds.Y)
UpdateScroll.CanvasSize = UDim2.new(0, 0, 0, UpdateTextLabel.TextBounds.Y + 10)


UpdateLogBtn.MouseButton1Click:Connect(function()
    UpdateModal.Visible = true
end)

UpdateCloseBtn.MouseButton1Click:Connect(function()
    UpdateModal.Visible = false
end)

local HubModal = Instance.new("Frame")
HubModal.Name = "HubModal"
HubModal.Size = UDim2.new(0, 350, 0, 220)
HubModal.Position = UDim2.new(0.5, -175, 0.5, -110)
HubModal.BackgroundColor3 = Color3.fromRGB(12, 9, 20)
HubModal.BackgroundTransparency = 0.15
HubModal.BorderSizePixel = 0
HubModal.Visible = false
HubModal.ZIndex = 15
HubModal.Parent = MainFrame

local HubModalCorner = Instance.new("UICorner")
HubModalCorner.CornerRadius = UDim.new(0, 10)
HubModalCorner.Parent = HubModal

local HubModalStroke = Instance.new("UIStroke")
HubModalStroke.Thickness = 2.5
HubModalStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
HubModalStroke.Parent = HubModal
ApplyPurpleWhiteBorderAnimation(HubModalStroke, 3)

local HubTopBar = Instance.new("Frame")
HubTopBar.Size = UDim2.new(1, 0, 0, 30)
HubTopBar.BackgroundColor3 = Color3.fromRGB(20, 14, 32)
HubTopBar.BackgroundTransparency = 0.2
HubTopBar.BorderSizePixel = 0
HubTopBar.ZIndex = 16
HubTopBar.Parent = HubModal

local HubTopCorner = Instance.new("UICorner")
HubTopCorner.CornerRadius = UDim.new(0, 10)
HubTopCorner.Parent = HubTopBar

local HubTitle = Instance.new("TextLabel")
HubTitle.Size = UDim2.new(1, -40, 1, 0)
HubTitle.Position = UDim2.new(0, 10, 0, 0)
HubTitle.BackgroundTransparency = 1
HubTitle.Text = "Hub & Creator Info"
HubTitle.TextSize = 14
HubTitle.Font = Enum.Font.Arcade
HubTitle.TextXAlignment = Enum.TextXAlignment.Left
HubTitle.ZIndex = 17
HubTitle.Parent = HubTopBar
MakeShinyPurpleText(HubTitle)

local HubCloseBtn = Instance.new("TextButton")
HubCloseBtn.Size = UDim2.new(0, 20, 0, 20)
HubCloseBtn.Position = UDim2.new(1, -25, 0, 5)
HubCloseBtn.BackgroundColor3 = Color3.fromRGB(180, 40, 60)
HubCloseBtn.BackgroundTransparency = 0.2
HubCloseBtn.Text = "X"
HubCloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
HubCloseBtn.TextSize = 12
HubCloseBtn.Font = Enum.Font.Arcade
HubCloseBtn.ZIndex = 17
HubCloseBtn.Parent = HubTopBar

local HubCloseCorner = Instance.new("UICorner")
HubCloseCorner.CornerRadius = UDim.new(0, 4)
HubCloseCorner.Parent = HubCloseBtn

local testerUserId = 0
pcall(function()
    testerUserId = Players:GetUserIdFromNameAsync("Tester_ugcAccount")
end)

local TesterAvatar = Instance.new("ImageLabel")
TesterAvatar.Size = UDim2.new(0, 55, 0, 55)
TesterAvatar.Position = UDim2.new(0.5, -27, 0, 35)
TesterAvatar.BackgroundColor3 = Color3.fromRGB(25, 18, 38)
TesterAvatar.Image = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. testerUserId .. "&width=150&height=150&format=png"
TesterAvatar.ZIndex = 16
TesterAvatar.Parent = HubModal

local TesterAvatarCorner = Instance.new("UICorner")
TesterAvatarCorner.CornerRadius = UDim.new(1, 0)
TesterAvatarCorner.Parent = TesterAvatar

local TesterAvatarStroke = Instance.new("UIStroke")
TesterAvatarStroke.Thickness = 2
TesterAvatarStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
TesterAvatarStroke.Parent = TesterAvatar
ApplyPurpleWhiteBorderAnimation(TesterAvatarStroke, 3)

local TesterNameLabel = Instance.new("TextLabel")
TesterNameLabel.Size = UDim2.new(1, 0, 0, 18)
TesterNameLabel.Position = UDim2.new(0, 0, 0, 94)
TesterNameLabel.BackgroundTransparency = 1
TesterNameLabel.Text = "Tester_ugcAccount"
TesterNameLabel.TextSize = 13
TesterNameLabel.Font = Enum.Font.Arcade
TesterNameLabel.ZIndex = 16
TesterNameLabel.Parent = HubModal
MakeShinyPurpleText(TesterNameLabel)

local TesterDescLabel = Instance.new("TextLabel")
TesterDescLabel.Size = UDim2.new(1, -20, 0, 28)
TesterDescLabel.Position = UDim2.new(0, 10, 0, 114)
TesterDescLabel.BackgroundTransparency = 1
TesterDescLabel.Text = "Hi it me gubby, I'm very sad because I lost this Roblox account and YouTube channel Gubby_scripter"
TesterDescLabel.TextSize = 10
TesterDescLabel.Font = Enum.Font.Arcade
TesterDescLabel.TextWrapped = true
TesterDescLabel.ZIndex = 16
TesterDescLabel.Parent = HubModal
MakeShinyPurpleText(TesterDescLabel)

local YTBtn = Instance.new("TextButton")
YTBtn.Name = "YTButton"
YTBtn.Size = UDim2.new(0, 140, 0, 32)
YTBtn.Position = UDim2.new(0.5, -145, 0, 165)
YTBtn.BackgroundColor3 = Color3.fromRGB(30, 20, 45)
YTBtn.BackgroundTransparency = 0.2
YTBtn.Text = "▶ YouTube"
YTBtn.TextSize = 12
YTBtn.Font = Enum.Font.Arcade
YTBtn.ZIndex = 16
YTBtn.Parent = HubModal
MakeShinyPurpleText(YTBtn)

local YTCorner = Instance.new("UICorner")
YTCorner.CornerRadius = UDim.new(0, 6)
YTCorner.Parent = YTBtn

local YTStroke = Instance.new("UIStroke")
YTStroke.Thickness = 1.5
YTStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
YTStroke.Parent = YTBtn
ApplyPurpleWhiteBorderAnimation(YTStroke, 4)

local DiscordBtn = Instance.new("TextButton")
DiscordBtn.Name = "DiscordButton"
DiscordBtn.Size = UDim2.new(0, 140, 0, 32)
DiscordBtn.Position = UDim2.new(0.5, 5, 0, 165)
DiscordBtn.BackgroundColor3 = Color3.fromRGB(30, 20, 45)
DiscordBtn.BackgroundTransparency = 0.2
DiscordBtn.Text = "💬 Discord"
DiscordBtn.TextSize = 12
DiscordBtn.Font = Enum.Font.Arcade
DiscordBtn.ZIndex = 16
DiscordBtn.Parent = HubModal
MakeShinyPurpleText(DiscordBtn)

local DiscordCorner = Instance.new("UICorner")
DiscordCorner.CornerRadius = UDim.new(0, 6)
DiscordCorner.Parent = DiscordBtn

local DiscordStroke = Instance.new("UIStroke")
DiscordStroke.Thickness = 1.5
DiscordStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
DiscordStroke.Parent = DiscordBtn
ApplyPurpleWhiteBorderAnimation(DiscordStroke, 4)

YTBtn.MouseButton1Click:Connect(function()
    CopyToClipboard("https://youtube.com/@gubby_scripter?si=vJqZM1LKKg1xgsr8")
    local origText = YTBtn.Text
    YTBtn.Text = "Copied YT Link!"
    task.wait(1.5)
    YTBtn.Text = origText
end)

DiscordBtn.MouseButton1Click:Connect(function()
    CopyToClipboard("https://discord.gg/k9U8gfTqc9")
    local origText = DiscordBtn.Text
    DiscordBtn.Text = "Copied Discord!"
    task.wait(1.5)
    DiscordBtn.Text = origText
end)

HomeBtn.MouseButton1Click:Connect(function()
    HubModal.Visible = true
end)

HubCloseBtn.MouseButton1Click:Connect(function()
    HubModal.Visible = false
end)

local ProfileFrame = Instance.new("Frame")
ProfileFrame.Name = "ProfileFrame"
ProfileFrame.Size = UDim2.new(1, -20, 0, 40)
ProfileFrame.Position = UDim2.new(0, 10, 0, 42)
ProfileFrame.BackgroundColor3 = Color3.fromRGB(22, 16, 32)
ProfileFrame.BackgroundTransparency = 0.3
ProfileFrame.BorderSizePixel = 0
ProfileFrame.Parent = MainFrame

local ProfileCorner = Instance.new("UICorner")
ProfileCorner.CornerRadius = UDim.new(0, 8)
ProfileCorner.Parent = ProfileFrame

local ProfileImage = Instance.new("ImageLabel")
ProfileImage.Size = UDim2.new(0, 32, 0, 32)
ProfileImage.Position = UDim2.new(0, 4, 0, 4)
ProfileImage.BackgroundColor3 = Color3.fromRGB(30, 24, 45)
ProfileImage.Image = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. LocalPlayer.UserId .. "&width=150&height=150&format=png"
ProfileImage.Parent = ProfileFrame

local AvatarCorner = Instance.new("UICorner")
AvatarCorner.CornerRadius = UDim.new(1, 0)
AvatarCorner.Parent = ProfileImage

local ProfileName = Instance.new("TextLabel")
ProfileName.Size = UDim2.new(1, -80, 0, 16)
ProfileName.Position = UDim2.new(0, 42, 0, 4)
ProfileName.BackgroundTransparency = 1
ProfileName.Text = "Welcome, " .. LocalPlayer.Name
ProfileName.TextSize = 13
ProfileName.Font = Enum.Font.Arcade
ProfileName.TextXAlignment = Enum.TextXAlignment.Left
ProfileName.Parent = ProfileFrame
MakeShinyPurpleText(ProfileName)

local ProfileExec = Instance.new("TextLabel")
ProfileExec.Size = UDim2.new(1, -80, 0, 14)
ProfileExec.Position = UDim2.new(0, 42, 0, 20)
ProfileExec.BackgroundTransparency = 1
ProfileExec.Text = "Using: " .. ExecutorName
ProfileExec.TextSize = 11
ProfileExec.Font = Enum.Font.Arcade
ProfileExec.TextXAlignment = Enum.TextXAlignment.Left
ProfileExec.Parent = ProfileFrame
MakeShinyPurpleText(ProfileExec)

local InfoButton = Instance.new("TextButton")
InfoButton.Name = "InfoButton"
InfoButton.Size = UDim2.new(0, 24, 0, 24)
InfoButton.Position = UDim2.new(1, -30, 0, 8)
InfoButton.BackgroundColor3 = Color3.fromRGB(35, 22, 55)
InfoButton.BackgroundTransparency = 0.2
InfoButton.Text = "?"
InfoButton.TextSize = 14
InfoButton.Font = Enum.Font.Arcade
InfoButton.Parent = ProfileFrame
MakeShinyPurpleText(InfoButton)

local InfoBtnCorner = Instance.new("UICorner")
InfoBtnCorner.CornerRadius = UDim.new(0, 6)
InfoBtnCorner.Parent = InfoButton

local InfoBtnStroke = Instance.new("UIStroke")
InfoBtnStroke.Thickness = 1.5
InfoBtnStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
InfoBtnStroke.Parent = InfoButton
ApplyPurpleWhiteBorderAnimation(InfoBtnStroke, 2.5)

local InfoModal = Instance.new("Frame")
InfoModal.Name = "InfoModal"
InfoModal.Size = UDim2.new(0, 350, 0, 220)
InfoModal.Position = UDim2.new(0.5, -175, 0.5, -110)
InfoModal.BackgroundColor3 = Color3.fromRGB(12, 9, 20)
InfoModal.BackgroundTransparency = 0.15
InfoModal.BorderSizePixel = 0
InfoModal.Visible = false
InfoModal.ZIndex = 10
InfoModal.Parent = MainFrame

local InfoModalCorner = Instance.new("UICorner")
InfoModalCorner.CornerRadius = UDim.new(0, 10)
InfoModalCorner.Parent = InfoModal

local InfoModalStroke = Instance.new("UIStroke")
InfoModalStroke.Thickness = 2.5
InfoModalStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
InfoModalStroke.Parent = InfoModal
ApplyPurpleWhiteBorderAnimation(InfoModalStroke, 3)

local InfoTopBar = Instance.new("Frame")
InfoTopBar.Size = UDim2.new(1, 0, 0, 30)
InfoTopBar.BackgroundColor3 = Color3.fromRGB(20, 14, 32)
InfoTopBar.BackgroundTransparency = 0.2
InfoTopBar.BorderSizePixel = 0
InfoTopBar.ZIndex = 11
InfoTopBar.Parent = InfoModal

local InfoTopCorner = Instance.new("UICorner")
InfoTopCorner.CornerRadius = UDim.new(0, 10)
InfoTopCorner.Parent = InfoTopBar

local InfoTitle = Instance.new("TextLabel")
InfoTitle.Size = UDim2.new(1, -40, 1, 0)
InfoTitle.Position = UDim2.new(0, 10, 0, 0)
InfoTitle.BackgroundTransparency = 1
InfoTitle.Text = "Player & Game Information"
InfoTitle.TextSize = 14
InfoTitle.Font = Enum.Font.Arcade
InfoTitle.TextXAlignment = Enum.TextXAlignment.Left
InfoTitle.ZIndex = 12
InfoTitle.Parent = InfoTopBar
MakeShinyPurpleText(InfoTitle)

local InfoCloseBtn = Instance.new("TextButton")
InfoCloseBtn.Size = UDim2.new(0, 20, 0, 20)
InfoCloseBtn.Position = UDim2.new(1, -25, 0, 5)
InfoCloseBtn.BackgroundColor3 = Color3.fromRGB(180, 40, 60)
InfoCloseBtn.BackgroundTransparency = 0.2
InfoCloseBtn.Text = "X"
InfoCloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
InfoCloseBtn.TextSize = 12
InfoCloseBtn.Font = Enum.Font.Arcade
InfoCloseBtn.ZIndex = 12
InfoCloseBtn.Parent = InfoTopBar

local InfoCloseCorner = Instance.new("UICorner")
InfoCloseCorner.CornerRadius = UDim.new(0, 4)
InfoCloseCorner.Parent = InfoCloseBtn

local InfoScroll = Instance.new("ScrollingFrame")
InfoScroll.Size = UDim2.new(1, -16, 1, -40)
InfoScroll.Position = UDim2.new(0, 8, 0, 34)
InfoScroll.BackgroundTransparency = 1
InfoScroll.BorderSizePixel = 0
InfoScroll.ScrollBarThickness = 3
InfoScroll.ScrollBarImageColor3 = Color3.fromRGB(160, 80, 255)
InfoScroll.CanvasSize = UDim2.new(0, 0, 0, 180)
InfoScroll.ZIndex = 11
InfoScroll.Parent = InfoModal

local InfoLayout = Instance.new("UIListLayout")
InfoLayout.SortOrder = Enum.SortOrder.LayoutOrder
InfoLayout.Padding = UDim.new(0, 4)
InfoLayout.Parent = InfoScroll

local function AddInfoRow(label, value, order)
    local Row = Instance.new("TextLabel")
    Row.Size = UDim2.new(1, -5, 0, 18)
    Row.BackgroundTransparency = 1
    Row.Text = "• " .. label .. ": " .. tostring(value)
    Row.TextSize = 11
    Row.Font = Enum.Font.Arcade
    Row.TextXAlignment = Enum.TextXAlignment.Left
    Row.LayoutOrder = order
    Row.ZIndex = 12
    Row.Parent = InfoScroll
    MakeShinyPurpleText(Row)
end

local CurrentGameName = "Unknown Game"
pcall(function()
    CurrentGameName = MarketplaceService:GetProductInfo(game.PlaceId).Name
end)

AddInfoRow("Display Name", LocalPlayer.DisplayName, 1)
AddInfoRow("Username", LocalPlayer.Name, 2)
AddInfoRow("User ID", LocalPlayer.UserId, 3)
AddInfoRow("Account Age", LocalPlayer.AccountAge .. " days", 4)
AddInfoRow("Game Name", CurrentGameName, 5)
AddInfoRow("Place ID", game.PlaceId, 6)
AddInfoRow("Job ID", string.sub(game.JobId ~= "" and game.JobId or "Singleplayer / Local", 1, 20) .. "...", 7)
AddInfoRow("Executor", ExecutorName, 8)

InfoButton.MouseButton1Click:Connect(function()
    InfoModal.Visible = true
end)

InfoCloseBtn.MouseButton1Click:Connect(function()
    InfoModal.Visible = false
end)
local SearchFrame = Instance.new("Frame")
SearchFrame.Name = "SearchFrame"
SearchFrame.Size = UDim2.new(1, -20, 0, 30)
SearchFrame.Position = UDim2.new(0, 10, 0, 88)
SearchFrame.BackgroundColor3 = Color3.fromRGB(22, 16, 32)
SearchFrame.BackgroundTransparency = 0.3
SearchFrame.BorderSizePixel = 0
SearchFrame.Parent = MainFrame

local SearchCorner = Instance.new("UICorner")
SearchCorner.CornerRadius = UDim.new(0, 6)
SearchCorner.Parent = SearchFrame

local SearchIcon = Instance.new("TextLabel")
SearchIcon.Size = UDim2.new(0, 25, 1, 0)
SearchIcon.BackgroundTransparency = 1
SearchIcon.Text = "🔍"
SearchIcon.TextSize = 12
SearchIcon.Parent = SearchFrame

local SearchBox = Instance.new("TextBox")
SearchBox.Size = UDim2.new(1, -30, 1, 0)
SearchBox.Position = UDim2.new(0, 25, 0, 0)
SearchBox.BackgroundTransparency = 1
SearchBox.PlaceholderText = "Search scripts..."
SearchBox.PlaceholderColor3 = Color3.fromRGB(150, 110, 200)
SearchBox.Text = ""
SearchBox.TextColor3 = Color3.fromRGB(240, 210, 255)
SearchBox.TextSize = 12
SearchBox.Font = Enum.Font.Arcade
SearchBox.TextXAlignment = Enum.TextXAlignment.Left
SearchBox.Parent = SearchFrame

local ScrollList = Instance.new("ScrollingFrame")
ScrollList.Name = "ScriptList"
ScrollList.Size = UDim2.new(1, -20, 0, 150)
ScrollList.Position = UDim2.new(0, 10, 0, 124)
ScrollList.BackgroundTransparency = 1
ScrollList.BorderSizePixel = 0
ScrollList.ScrollBarThickness = 4
ScrollList.ScrollBarImageColor3 = Color3.fromRGB(150, 80, 255)
ScrollList.CanvasSize = UDim2.new(0, 0, 0, 0)
ScrollList.Parent = MainFrame

local ListLayout = Instance.new("UIListLayout")
ListLayout.SortOrder = Enum.SortOrder.LayoutOrder
ListLayout.Padding = UDim.new(0, 6)
ListLayout.Parent = ScrollList

ListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    ScrollList.CanvasSize = UDim2.new(0, 0, 0, ListLayout.AbsoluteContentSize.Y + 10)
end)

local scriptButtons = {}

local function AddSectionHeader(title, layoutOrder)
    local Header = Instance.new("TextLabel")
    Header.Size = UDim2.new(1, -5, 0, 20)
    Header.BackgroundTransparency = 1
    Header.Text = "━━━ " .. title .. " ━━━"
    Header.TextSize = 13
    Header.Font = Enum.Font.Arcade
    Header.LayoutOrder = layoutOrder
    Header.Parent = ScrollList
    MakeShinyPurpleText(Header)
end

local function RunScript(url)
    local success, err = pcall(function() loadstring(game:HttpGet(url))() end)
    if not success then warn("Execution Error: " .. tostring(err)) end
end

local function AddScriptButton(name, url, layoutOrder)
    local Btn = Instance.new("TextButton")
    Btn.Name = name
    Btn.Size = UDim2.new(1, -5, 0, 30)
    Btn.BackgroundColor3 = Color3.fromRGB(26, 18, 38)
    Btn.BackgroundTransparency = 0.2
    Btn.Text = name
    Btn.TextSize = 12
    Btn.Font = Enum.Font.Arcade
    Btn.LayoutOrder = layoutOrder
    Btn.Parent = ScrollList
    MakeShinyPurpleText(Btn)

    local BtnCorner = Instance.new("UICorner")
    BtnCorner.CornerRadius = UDim.new(0, 6)
    BtnCorner.Parent = Btn

    local BtnStroke = Instance.new("UIStroke")
    BtnStroke.Thickness = 1.5
    BtnStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    BtnStroke.Parent = Btn
    ApplyPurpleWhiteBorderAnimation(BtnStroke, 5)

    Btn.MouseEnter:Connect(function()
        TweenService:Create(Btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(50, 32, 80), BackgroundTransparency = 0.1}):Play()
    end)

    Btn.MouseLeave:Connect(function()
        TweenService:Create(Btn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(26, 18, 38), BackgroundTransparency = 0.2}):Play()
    end)

    Btn.MouseButton1Click:Connect(function()
        RunScript(url)
    end)

    table.insert(scriptButtons, Btn)
end

local order = 1
AddSectionHeader("INSTANT UGC", order) order = order + 1
AddScriptButton("Tower  of misery", "https://pastebin.com/raw/nFuqw4aA", order) order = order + 1
AddScriptButton("GoGo Climbing [Obby Team]", "https://pastefy.app/3aXlq5fq/raw", order) order = order + 1

AddSectionHeader("EASY UGC", order) order = order + 1
AddScriptButton("AFK For UGC 🎃", "https://pastefy.app/GTTskdF2/raw", order) order = order + 1
AddScriptButton("MUSINSA STREET", "https://pastefy.app/JF6PKooD/raw", order) order = order + 1
AddScriptButton("[NEW] Taro Land: Merdeka Fest", "https://pastefy.app/78MgOu8a/raw", order) order = order + 1

AddSectionHeader("GRINDY UGC", order) order = order + 1
AddScriptButton("AVL: The Show 2", "https://pastefy.app/veON2ePW/raw", order) order = order + 1
AddScriptButton("Get Your Avatar! ✨", "https://pastefy.app/IUL0xlQK/raw", order) order = order + 1
AddScriptButton("Earn UGC Obby (AFK or play)", "https://pastefy.app/SU7cvcP3/raw", order) order = order + 1
AddScriptButton("[Free UGC] Nutty Wizards ✨RNG✨", "https://pastefy.app/558j0kga/raw", order) order = order + 1
AddScriptButton("FREE UGC Grind 2 (Obby • PvP • AFK)", "https://pastefy.app/SUHwyvoE/raw", order) order = order + 1
AddScriptButton("Dixyland", "https://pastefy.app/nSgkDrE0/raw", order) order = order + 1

AddSectionHeader("OTHER", order) order = order + 1
AddScriptButton("Whyborn", "https://pastefy.app/lND8cWtT/raw", order) order = order + 1
AddScriptButton("INF YEILD", "https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source", order) order = order + 1
AddScriptButton("FAKE PURCHASE", "https://pastefy.app/Ow3Ibi7U/raw", order) order = order + 1

SearchBox:GetPropertyChangedSignal("Text"):Connect(function()
    local query = string.lower(SearchBox.Text)
    for _, btn in ipairs(scriptButtons) do
        if query == "" or string.find(string.lower(btn.Text), query, 1, true) then
            btn.Visible = true
        else
            btn.Visible = false
        end
    end
end)

local function EnableDragging(frame, handle)
    local dragging = false
    local dragStart = nil
    local startPos = nil

    handle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end)

    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = false
        end
    end)
end

EnableDragging(MainFrame, TopBar)
EnableDragging(ToggleBtn, ToggleBtn)
